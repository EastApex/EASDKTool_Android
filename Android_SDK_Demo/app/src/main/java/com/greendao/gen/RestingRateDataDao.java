package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.apex.sdk.db.resting.RestingRateData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RESTING_RATE_DATA".
*/
public class RestingRateDataDao extends AbstractDao<RestingRateData, Long> {

    public static final String TABLENAME = "RESTING_RATE_DATA";

    /**
     * Properties of entity RestingRateData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Time_stamp = new Property(0, long.class, "time_stamp", true, "_id");
        public final static Property HeartRate = new Property(1, int.class, "heartRate", false, "HEART_RATE");
        public final static Property CurrentTime = new Property(2, long.class, "currentTime", false, "CURRENT_TIME");
    }


    public RestingRateDataDao(DaoConfig config) {
        super(config);
    }
    
    public RestingRateDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RESTING_RATE_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: time_stamp
                "\"HEART_RATE\" INTEGER NOT NULL ," + // 1: heartRate
                "\"CURRENT_TIME\" INTEGER NOT NULL );"); // 2: currentTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RESTING_RATE_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, RestingRateData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTime_stamp());
        stmt.bindLong(2, entity.getHeartRate());
        stmt.bindLong(3, entity.getCurrentTime());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, RestingRateData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTime_stamp());
        stmt.bindLong(2, entity.getHeartRate());
        stmt.bindLong(3, entity.getCurrentTime());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public RestingRateData readEntity(Cursor cursor, int offset) {
        RestingRateData entity = new RestingRateData( //
            cursor.getLong(offset + 0), // time_stamp
            cursor.getInt(offset + 1), // heartRate
            cursor.getLong(offset + 2) // currentTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, RestingRateData entity, int offset) {
        entity.setTime_stamp(cursor.getLong(offset + 0));
        entity.setHeartRate(cursor.getInt(offset + 1));
        entity.setCurrentTime(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(RestingRateData entity, long rowId) {
        entity.setTime_stamp(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(RestingRateData entity) {
        if(entity != null) {
            return entity.getTime_stamp();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(RestingRateData entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
