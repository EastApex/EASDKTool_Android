package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.apex.sdk.db.pace.PaceData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PACE_DATA".
*/
public class PaceDataDao extends AbstractDao<PaceData, Long> {

    public static final String TABLENAME = "PACE_DATA";

    /**
     * Properties of entity PaceData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property CurrentTime = new Property(0, long.class, "currentTime", true, "_id");
        public final static Property StepPace = new Property(1, int.class, "stepPace", false, "STEP_PACE");
    }


    public PaceDataDao(DaoConfig config) {
        super(config);
    }
    
    public PaceDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PACE_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: currentTime
                "\"STEP_PACE\" INTEGER NOT NULL );"); // 1: stepPace
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PACE_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PaceData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCurrentTime());
        stmt.bindLong(2, entity.getStepPace());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PaceData entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCurrentTime());
        stmt.bindLong(2, entity.getStepPace());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public PaceData readEntity(Cursor cursor, int offset) {
        PaceData entity = new PaceData( //
            cursor.getLong(offset + 0), // currentTime
            cursor.getInt(offset + 1) // stepPace
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PaceData entity, int offset) {
        entity.setCurrentTime(cursor.getLong(offset + 0));
        entity.setStepPace(cursor.getInt(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PaceData entity, long rowId) {
        entity.setCurrentTime(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PaceData entity) {
        if(entity != null) {
            return entity.getCurrentTime();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PaceData entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
